# 2026-08-05

# DAY 29：Week 4 Day 10 — 继续推进 monad-liq-mvp 编码

## 今日任务链路

今天把编码重心放回 `monad-liq-mvp` 本地仓库，继续补 Week 4 Hackathon Start Card 里承诺的 User 轨能力。

|| 顺序 | 任务 | 产出 |
||------|------|------|
|| 1 | 检查本地代码未提交变更 | `.gitignore` / Chrome 插件 / `package.json` / `public/user.html` / `vercel.json` |
|| 2 | 继续 User 保护台开发 | `public/user.html` 四段式 UX + deep-link focus |
|| 3 | Chrome 插件修复 | `background.js` 异步 `loadSettings()` + `optional_host_permissions` 支持 `http://*/*` |
|| 4 | 补 protect dashboard build 脚本 | `package.json` 新增 `build:protect` |
|| 5 | 对齐 Vercel 配置 | `vercel.json` 适配 protect dashboard 路由 |

## 1. 本地代码当前状态

本地路径：`C:\Users\gg\monad-liq-mvp`

未提交变更：
- `.gitignore`：新增 `public/user/` 和 `apps/protect/install.pid`
- `extensions/monad-liq-guard/background.js`：把 `chrome.storage.local.get(...).then(...)` 改成 `await loadSettings()` 异步写法，修复 notification click 时 `apiBase` 读取
- `extensions/monad-liq-guard/manifest.json`：`optional_host_permissions` 从 `https://*/*` 改成 `http://*/*, https://*/*`，支持本地 `http://localhost:8787`
- `package.json`：新增 `build:protect` script
- `public/user.html`：保留四段式 UX + deep-link `focus=worst`，删掉冗余 inline 样式
- `vercel.json`：适配 protect dashboard 静态路由

## 2. 本周对 Hackathon 的实际推进

对照 `tasks/week4/hackathon-start-card.md`：

| 条目 | 状态 |
|------|------|
| Aave + Euler 双协议健康因子只读扫描 | 进行中：Aave 已有，Euler 地址待确认 |
| Risk Rating（B/C/D）单协议版本 | 未开始：等 `api/index.ts` 路由接入 |
| Stress Test（-10% / -30%） | 未开始：依赖 Risk Rating |
| 清算交易草稿生成 | 已有：`LiquidationRouter.sol` Aave path |
| AI Explanation 规则模板 | 未开始 |
| User 保护台 / Chrome 插件 | 进行中：`public/user.html` + `extensions/monad-liq-guard` 已有骨架 |

## 3. 关键认知

- **Chrome 插件在 Hackathon 里是“加分展示项”，不是必做项。** 先把 API happy path 跑通，插件只作为延伸展示。
- **User 轨和 Liquidator 轨可以共用同一份 `/scan` 结果。** 不需要为两类用户写两套扫描器，只需要在输出层加一层 Risk Rating / AI Explanation 包装。
- **Protect dashboard 用 Vite build 输出到 `public/user/` 是正确路径。** Vercel 上可以直接当静态资源服务，不需要额外后端。

## 4. 产出链接

- Hackathon 项目：https://github.com/MonadTiya/monad-liq-mvp
- Issues：https://github.com/MonadTiya/monad-liq-mvp/issues
- Hackathon Start Card：`tasks/week4/hackathon-start-card.md`
- 需求检查：`tasks/week4/requirement-check.md`
- 团队复盘：`tasks/week3/hackathon-retro.md`
- 学习记录：https://github.com/tiyadegure/monad-builder-camp

## 5. 下一步

### 明天（Day 11）：
- commit `monad-liq-mvp` 本地变更并推送到 GitHub
- 在 `api/index.ts` 里补 Risk Rating / Stress Test 路由
- 用 Aave 真实地址跑通第一条端到端 JSON

### Day 12-13：
- Euler 只读扫描接入
- AI Explanation 规则模板接入
- 整理 2 分钟 Demo 脚本

### Day 14-15：
- 用户测试 + Demo 录屏
- README + 提交材料打包
